<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- 
		目的：iOS 應用程式的屬性清單 (Property List)
		作用：定義應用程式的元數據，如顯示名稱、版本、包名 (Bundle ID)、系統權限申請說明（如位置權限）及支援的螢幕方向。
		格式與用法：XML 格式。在新增需要原生權限的插件後，必須在此處添加對應的權限說明字串。
	-->

	<!-- 是否停用手機上的最小偵間隔限制（用於支援 120Hz 高刷新率螢幕） -->
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<!-- 開發語言區域 -->
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<!-- 應用顯示名稱 -->
	<key>CFBundleDisplayName</key>
	<string>Ntpc Garbage Map</string>
	<!-- 可執行檔名 -->
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<!-- 應用唯一識別碼 (Bundle Identifier) -->
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<!-- Info.plist 字典版本 -->
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<!-- 專案名稱 -->
	<key>CFBundleName</key>
	<string>ntpc_garbage_map</string>
	<!-- 包類型 (APPL 代表應用程式) -->
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<!-- 應用的版本號碼（顯示給用戶看，通常對應 pubspec.yaml 的 version 前半部） -->
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<!-- 簽署標識符 -->
	<key>CFBundleSignature</key>
	<string>????</string>
	<!-- 內部版本代碼（用於商店更新，對應 pubspec.yaml 的 build number） -->
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<!-- 是否要求在 iOS 系統上執行 -->
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- 「使用中」定位權限說明：向用戶解釋為什麼需要定位 -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>我們需要您的位置來顯示您附近的垃圾車。</string>
	<!-- 「始終」定位權限說明（如需背景定位時使用） -->
	<key>NSLocationAlwaysUsageDescription</key>
	<string>我們需要您的位置來提供即時的垃圾車靠近通知。</string>
	<!-- 網路安全配置：允許載入非 HTTPS 的資源 -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
	<!-- 是否支援間接輸入裝置（如滑鼠、觸控板） -->
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<!-- 啟動畫面 Storyboard 名稱 -->
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<!-- 主介面 Storyboard 檔案名稱 -->
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- 支援的螢幕方向（手機） -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<!-- 支援的螢幕方向（iPad） -->
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
