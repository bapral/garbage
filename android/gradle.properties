# 目的：Gradle 執行環境屬性設定 (Gradle Properties)
# 作用：調整 Gradle 建置過程中的 JVM 參數（如記憶體上限），並啟用全域特性（如 AndroidX 支援），以優化建置效能與相容性。
# 格式與用法：使用簡單的「鍵=值」對形式。每行代表一個配置選項。

org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
